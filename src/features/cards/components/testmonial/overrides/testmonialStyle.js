import { styled } from "@mui/material/styles";
import { Card } from "@mui/material";
const TestmonialStyled = styled(Card)(({ theme }) => ({
  background: "transparent",
  boxShadow: "none",
  border: "none",
}));
export default TestmonialStyled;
